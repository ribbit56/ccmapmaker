/*
  Generation pipeline steps (CLAUDE.md §5): heightmap, biomes, hydrology,
  settlements, roads, naming, labels — each a pure `(World, GenConfig, rng) => World`
  so any one can re-run independently. Placeholders land here from Phase 1 onward.
*/
export {};
